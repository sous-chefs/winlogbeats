---
driver:
  name: vagrant
  chef_version: <%= ENV['CHEF_VERSION'] || 'current' %>
  env: [CHEF_LICENSE=accept]

suites:
  - name: default
    run_list:
      - recipe[test::default]

platforms:
  - name: tas50/windows_2019

verifier: inspec
